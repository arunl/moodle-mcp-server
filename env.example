# Moodle MCP Server Configuration
# Copy this file to .env and fill in your values

# Required: Your Moodle site URL (without trailing slash)
MOODLE_URL=https://moodle.example.edu

# ============================================
# AUTHENTICATION - Choose ONE of the following:
# ============================================

# Option 1: Token (Best for Moodle with web services enabled)
# Get this from: Moodle → User Menu → Preferences → Security Keys
# MOODLE_TOKEN=your_token_here

# Option 2: Username/Password (for non-SSO Moodle instances only)
# Only works if your Moodle allows direct login (not SSO)
# MOODLE_USERNAME=your_username
# MOODLE_PASSWORD=your_password

# Option 3: Session-based (for SSO/university Moodle where tokens are disabled)
# Use this if your university uses SSO and has disabled token generation
# 
# To get these values:
# 1. Log in to Moodle in your browser
# 2. Open Developer Tools (F12) → Application → Cookies
# 3. Find "MoodleSession" cookie and copy its value
# 4. On any Moodle page, open Console and type: M.cfg.sesskey
# 
# Note: Sessions expire! You'll need to refresh these periodically.
# MOODLE_SESSION=your_moodle_session_cookie
# MOODLE_SESSKEY=your_sesskey

# Optional: Service name (default: moodle_mobile_app)
# Most Moodle instances use moodle_mobile_app
# MOODLE_SERVICE=moodle_mobile_app
